# prisma-testing

This repo is to play around testing with prisma with a temporary database.

# Run the tests

-   clone the repository

execute

```
npm install
cp .env.example .env
npm run test
```
